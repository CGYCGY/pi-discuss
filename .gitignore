node_modules/
*.log
logs/

# The live panel is personal machine config — which models this install resolves and
# pays for. panel.yaml.example is the tracked twin.
panel.yaml

# Discussion artifacts are written into the *user's* cwd at runtime; ignore any
# that land here from a local run.
discussions/

# Live smoke runs stage a scratch pi workspace here (kept only on failure).
.smoke/

# TypeScript build artifacts (we run noEmit, but just in case)
*.tsbuildinfo
dist/

# Editor / OS noise
.DS_Store
*.swp
.idea/
.vscode/
