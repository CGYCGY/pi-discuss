{
  "name": "pi-discuss",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "license": "MIT",
  "description": "A pi extension that convenes a panel of 2-5 agents on different frontier models: they answer independently, debate across rounds, and the host session synthesizes the divergence map.",
  "engines": {
    "bun": ">=1.4.0"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "bun test test/"
  },
  "dependencies": {
    "yaml": "^2.9.0"
  },
  "devDependencies": {
    "@earendil-works/pi-coding-agent": "^0.84.3",
    "@earendil-works/pi-tui": "^0.84.3",
    "typebox": "1.3.7",
    "@types/bun": "^1.4.0",
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0"
  }
}
